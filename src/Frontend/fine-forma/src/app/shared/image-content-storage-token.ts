import { InjectionToken } from '@angular/core';

import { IImageContentStorage } from 'fine-forma-core';

export const IMAGE_CONTENT_STORAGE = new InjectionToken<IImageContentStorage>('image-content-storage');